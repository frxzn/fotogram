import React from 'react';

interface Props {}

const Navbar: React.FC<Props> = () => {
  return (
    <header>
      <nav>
        <div></div>
      </nav>
    </header>
  );
};

export default Navbar;
